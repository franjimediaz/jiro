// utils/iconMap.js
import {
  Home,
  User,
  Settings,
  AlarmClock,
  Calendar,
  ClipboardList,
  CheckCircle,
  FileText,
  Phone,
  Mail,
  
} from 'lucide-react';

export const iconMap = {
  
  Home,
  User,
  Settings,
  AlarmClock,
  Calendar,
  ClipboardList,
  CheckCircle,
  FileText,
  Phone,
  Mail
};

export const iconNames = Object.keys(iconMap); // ['Home', 'User', 'Settings', ...]
